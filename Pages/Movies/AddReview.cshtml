@page
@model AddReviewModel

@{
    ViewData["Title"] = "Add Review";
}

<h1>Create</h1>

<h4>Review</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div class="form-group">
                <label asp-for="Review.MovieId" class="control-label"></label>
                <select asp-for="Review.MovieId" asp-items="Model.MoviesDropDown" class="form-control">
                    <option value="">--Select Movie--</option> 
                </select>
                <span asp-validation-for="Review.MovieId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Review.Score" class="control-label"></label>
                <input asp-for="Review.Score" class="form-control" />
                <span asp-validation-for="Review.Score" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Add Review" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}